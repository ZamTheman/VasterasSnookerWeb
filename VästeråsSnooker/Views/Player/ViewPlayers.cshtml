@model VästeråsSnooker.Models.ViewModels.IViewPlayersViewModel
@{
    ViewBag.Title = "View";
}

<h3>Spelare</h3>

</a>
<div class="row container">
    <div class="col-sm-4" id="playersListId">
        <ul class="list-group">
            <a href="#" onclick="createPlayer()">
                <li class="list-group-item">Ny spelare</li>
            </a>
                @{
                    foreach (var player in Model.AllPlayers)
                    {                       
                        <a href="#" onclick="getPlayer(@player.Id)">
                            <li class="list-group-item">@player.Name</li>
                        </a>
                    }
                }
        </ul>
    </div>
    <div id="playerDetails" class="col-sm-8"></div>
       
</div>
<script src="~/Scripts/SiteScripts/PlayerScripts.js"></script>
